{
  "name": "pixel-drawing",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "source": "src/client/index.html",
  "scripts": {
    "build:client": "parcel build --dist-dir=dist/public",
    "build": "rimraf dist && parcel build --dist-dir=dist/public && npx tsc -p ./tsconfig.json",
    "start": "node dist/index.js",
    "prettier": "prettier --config .prettierrc --write .",
    "dev:server": "npx tsc -p ./tsconfig.json && node dist/index.js",
    "dev:client": "parcel --dist-dir=dist/public"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3",
    "rimraf": "^5.0.1",
    "socket.io": "^4.7.1",
    "socket.io-client": "^4.7.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.3",
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2",
    "buffer": "^6.0.3",
    "parcel": "^2.9.3",
    "prettier": "^3.0.0",
    "typescript": "^5.1.6"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
